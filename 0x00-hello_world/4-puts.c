#include <stdio.h>

/*
 * main - entry
 *
 * Return is always 0
 * puts print output to screen
 */
int main(void)

{
	char alx[] = "\"Programming is like building a multilingual puzzle";

	puts(alx);
	return (0);
}
