#include <stdio.h>

/**
 * main - entry
 *
 * Return: is always 0
*/

int main(void)
i
{
	char alx[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", alx);
	return (0);
}
